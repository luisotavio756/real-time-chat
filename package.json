{
  "name": "socketio",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "private": true,
  "scripts": {
    "dev:api": "cd packages/server && yarn dev:server",
    "dev:client": "cd packages/client && yarn start",
    "lint:client": "cd packages/client && yarn eslint --ext .ts,.tsx --fix ."
  },
  "license": "MIT",
  "devDependencies": {
    "@monorepo/eslint-config": "*",
    "typescript": "^4.0.3"
  }
}
